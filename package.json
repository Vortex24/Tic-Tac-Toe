{
  "name": "tic-tac-toe-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./src/server.js",
    "build": "esbuild src/* --bundle --outdir=dist --platform=node",
    "dev": "concurrently \"npm run server\" \"npm run watch\"",
    "lint": "eslint .",
    "server": "nodemon ./src/server.js",
    "watch": "npm run build -- --watch"
  },
  "author": "vortex",
  "license": "ISC",
  "description": "",
  "type": "module",
  "dependencies": {
    "concurrently": "^9.0.1",
    "dotenv": "^16.4.5",
    "esbuild": "^0.23.1",
    "eslint": "^9.10.0",
    "express": "^4.21.0",
    "mongodb": "^6.9.0",
    "nodemon": "^3.1.5"
  }
}
